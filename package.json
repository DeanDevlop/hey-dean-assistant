{
  "name": "hey-dean-assistant",
  "version": "1.0.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "next": "14.0.4", // Pinned to a specific version
    "react": "18.2.0", // Pinned to a specific version
    "react-dom": "18.2.0", // Pinned to a specific version
    "express-rate-limit": "7.1.5" // Added for rate limiting
  }
}